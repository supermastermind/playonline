extended check that there are no "\r"

normal replace "= " by "="
normal replace " =" by "="
normal replace " !" by "!"
normal replace " || " by "||"
normal replace " && " by "&&"
normal replace "if (" by "if("
normal replace "; " by ";"
normal replace " + " by "+"

regexp replace //(.*) by nothing
delete trailing spaces
delete leading spaces
regexp replace (\n)+ by \n
extended replace "\n}" by "}"
manual deletion of comments:
- let precalculation_mode=
- let nbCodesToGoThrough=
- if ( (next_cur_game_idx==1)
delete trailing spaces
delete leading spaces