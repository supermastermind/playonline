extended check that there are no "\r"

normal replace "= " by "="
normal replace " =" by "="
normal replace " !" by "!"
normal replace " || " by "||"
normal replace " && " by "&&"
normal replace "if (" by "if("
normal replace "; " by ";"
normal replace ", " by ","
normal replace " + " by "+"
normal replace " {" by "{"
normal replace " <" by "<"
normal replace "< " by "<"
normal replace " >" by ">"
normal replace "> " by ">"

regexp replace //(.*) by nothing => PB with HTTPS://!
delete trailing spaces
delete leading spaces
regexp replace (\n)+ by \n

extended replace "\n}" by "}" => PB if // comments are left!

For GameSolver.js: manual deletion of comments:
- let precalculation_mode=
- let nbCodesToGoThrough=
- if((depth<=1)&&(!compute_sum_ini)){
- if( (next_cur_game_idx==1)
